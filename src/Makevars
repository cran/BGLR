SOURCES=$(wildcard *.c)

OBJECTS = $(SOURCES:.c=.o)

PKG_LIBS =  $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
#PKG_LIBS = $(SHLIB_OPENMP_CFLAGS)

PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS) -I.
  
all: $(SHLIB)

clean:
	@rm -f $(OBJECTS)
